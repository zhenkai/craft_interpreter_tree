fun fib(n) {
  if (n < 2) return n;
  return fib(n - 1) + fib(n - 2); 
}

var before = clock();
print fib(32); 
var after = clock();
// print something like 98 on my laptop.. slow as f**k..
print after - before;
